<!doctype html>
<html>
  <head>
    <title>Sinatra</title>
  </head>
  <body>
    <p>Time is: <%= Time.now %></p>
    <% @list.each do |item| %>
      <p><%= item.title %></p>
    <% end %>
    <input type="text"></input>
  </body>
</html>
